import { ScrubBarThumbnailsDirective } from './scrub-bar-thumbnails.directive';

describe('ScrubBarThumbnailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrubBarThumbnailsDirective();
    expect(directive).toBeTruthy();
  });
});
