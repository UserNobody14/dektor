import { PostNumbersPipe } from './post-numbers.pipe';

describe('PostNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new PostNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
